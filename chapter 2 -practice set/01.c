#include <stdio.h>

int main() {

    int a ,b=a;//a has garbage value so that 's why b also has garbage value
    a=2;
    printf("%d %d",a,b);
    int v =3*3;

    char dt='2';
    return 0;
}